import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

final loginPageInputUsername = find.byKey(const Key('username'));
final loginPageInputPassword = find.byKey(const Key('password'));
final loginPageButtonLogin = find.text('Iniciar Sesión');
